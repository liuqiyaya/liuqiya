# 学习笔记
## 本周学习b站课程
springboot3+vue3全栈开发
## 具体学习内容
1.springboot基础知识,包括项目创建和配置,目录结构等

2.实战篇:学习了前期开发模式和环境搭建,登录,注册,文章增删改查

3.jwt令牌,拦截器,统一异常处理器

实现方式
```java
package com.liuqiya.dormitoryrepairsystem.utils;

import com.auth0.jwt.JWT;
import com.auth0.jwt.algorithms.Algorithm;
import java.util.Date;
import java.util.Map;

public class JwtUtil {
    private static final String KEY = "liuqiya";

    //接收业务数据,生成token并返回
    public static String genToken(Map<String, Object> claims) {
        return JWT.create()
                .withClaim("claims", claims)
                .withExpiresAt(new Date(System.currentTimeMillis() + 1000 * 60 * 60 * 12))
                .sign(Algorithm.HMAC256(KEY));
    }

    //接收token,验证token,并返回业务数据
    public static Map<String, Object> parseToken(String token) {
        return JWT.require(Algorithm.HMAC256(KEY))
                .build()
                .verify(token)
                .getClaim("claims")
                .asMap();
    }
}

```
存储方式
```java
public class ThreadLocalUtil {
    private static final ThreadLocal THREAD_LOCAL = new ThreadLocal();
    
    // 获取存储的值
    public static <T> T get() {
        return (T) THREAD_LOCAL.get();
    }
    
    // 存储值
    public static void set(Object value) {
        THREAD_LOCAL.set(value);
    }
    
    // 清除存储的值（防止内存泄漏）
    public static void remove() {
        THREAD_LOCAL.remove();
    }
}

```
4.vue指令和element组件以及vue工程化开发流程和api风格等等
笔记在飞书

[飞书学习笔记](https://kcnx16lwgwxf.feishu.cn/wiki/BxIhwzFFPiyx7fk7cw0cMWKenwb?fromScene=spaceOverview)

